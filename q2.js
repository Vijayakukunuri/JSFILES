// with duplicates//
let phrase="finding vowels in a phrase";
// using for in//
for(let i in phrase){
    let c =phrase[i]
    if(c==='a'||c==='e'||c==='i'||c==='o'||c==='u'){
        console.log(c);
    }
}